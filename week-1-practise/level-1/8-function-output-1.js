function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? this program chose by the (Math.random() method a number which is between 0,1 randomly and at can be any number betwen 0 and 1 but not 1.)multiblied by 10 so the result as any number between 0 and 1 *10
// - How many parameters does the function take?0 or no difinet parameters
// - What is the function name?getNumber
// - Where is the function called?in the var and named result   with what parameters?
