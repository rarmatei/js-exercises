function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code ", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them. the function joins the 2 parameter strings  into one line
// - How many parameters does the function take? 2 (w1 ,w2)
// - What is the function name?  s
// - Where is the function called? var result  with what parameters? ("code","your")
// - What does the function return? codeyour
// - How can this function be improved? adding spaces inside first string parameter  ("code ") the return will be (code your)
